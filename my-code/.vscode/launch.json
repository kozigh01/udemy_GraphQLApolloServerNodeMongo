{
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Docker: Attach to Node",
      "port": 9229,
      "address": "localhost",
      "localRoot": "${workspaceFolder}/",
      "remoteRoot": "/node/app",
      // "processId": "${command:PickProcess}",
      "restart": true,
      "protocol": "inspector"
    }
  ]
}